name: Trigger n8n workflow

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping n8n webhook
        run: curl -X GET "https://n8n-dhne.onrender.com/webhook/YOUR_WEBHOOK_ID"
